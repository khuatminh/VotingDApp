# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/

# Env
.env
.env.*
!.env.example

# Node
node_modules/

# Foundry artifacts (contracts/.gitignore covers the rest)
**/cache/
**/out/
**/broadcast/
!contracts/broadcast/.gitkeep

# Frontend ABIs are auto-synced by scripts/sync-abi.sh and committed so
# hosted builds (Vercel etc.) can resolve them without forge.

# Superpowers ephemeral state (brainstorm mockups, agent state)
.superpowers/
